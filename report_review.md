# DMA Project #2 — 보고서 수정·보완 제안 (Team 22)

---

## 오탈자 수정 (PDF 원본 확인 필수)

PDF→MD 변환 아티팩트로 추정되나, 제출 PDF 원본에서도 동일하면 감점 요인.

| 위치 | 현재 | 수정 |
|------|------|------|
| PART I 양의연관 표 헤더 | `confdence` | `confidence` |
| PART I 양의연관 표 헤더 | `lif` | `lift` |
| PART I 양의연관 표 | `tme-series` | `time-series` |
| PART I 양의연관 표 | `forecastng` | `forecasting` |
| PART III 모델 표 | `MultnomialNB` | `MultinomialNB` |
| 전체 본문 | 단어 간 공백 누락 다수 (`table을생성하여` 등) | 띄어쓰기 전체 점검 |

---

## PART I — 연관분석 보완

### 1. 음의연관 예시 3개 추가 or 선택 근거 명시
- 현재: lift < 1 규칙 6개 중 3개만 예시 제시
- 보완: 나머지 3개도 표에 추가하거나, "대표 3개를 선택한 기준(lift 낮은 순, 해석 가능성 등)" 한 문장 명시

### 2. 임계값별 규칙 수 변화에 정성 해석 추가
- 현재: 표 + "급격히 감소" 한 줄
- 보완 예시:
  > lift 임계값 10.0에서 남는 규칙 2개(anova↔repeated-measures, arima↔time-series)는
  > 해당 태그 쌍이 단순 동시출현을 넘어 의미적으로 강하게 결합된 쌍임을 시사한다.
  > 반면 lift 1.0~1.5 구간에서 규칙 수가 40→30으로 감소하는 것은, 약한 양의연관 규칙
  > 다수가 이 구간에 집중되어 있음을 의미한다.

---

## PART II — 검색엔진 보완

### 1. 성능 개선 보고서 추가 (5점 — 현재 미작성)

보고서에 아직 없는 섹션. 아래 내용을 추가해야 점수 확보 가능.

#### 1-1. 추가 시도 방법 비교표

| 방법 | 이론 출처 | 평균 BPREF | 판정 |
|------|---------|-----------|------|
| BM25F baseline | Ch.12 s.22 | 0.2504 | 기준 |
| BM25+ (b=0.80, k1=1.45, δ=2.25) | Ch.12 s.22 | 0.2624 | ✅ 채택 |
| BM25+ + StemmingAnalyzer 재색인 | — | 0.2767 | ✅ 채택 |
| 파라미터 미세조정 (최종) | — | **0.2780** | ✅ 채택 |
| Dirichlet smoothing (μ=2500) | Ch.12 s.33 | 0.2767 | ❌ 기각 |
| JM smoothing (λ=0.1~0.7) | Ch.12 s.31 | 0.224~0.252 | ❌ 기각 |
| BM25+ + JM blend (10~40% JM) | Ch.12 s.31 | 0.267~0.276 | ❌ 기각 |
| PRF key_terms (top3~5, n=1~3) | Ch.12 s.38 | 0.273~0.278 | ❌ 기각 |
| OrGroup factor 변경 (0.1~0.9) | TA05 s.25 | 0.278 | 무효과 |

#### 1-2. 언어 모델 계열 기각 이유 서술 예시

> Dirichlet smoothing(μ=2500)과 JM smoothing(λ=0.1~0.7)을 intappscorer 내에
> 구현하여 비교하였다. 두 방법 모두 BM25+(0.278) 대비 성능이 저하되었다.
> 원인을 분석하면, 본 컬렉션(2,772개 학술 논문)은 문서 길이 편차가 크고
> 도메인 특수 용어의 출현 빈도(cf)가 매우 낮아, 컬렉션 확률 P(w|C)로의
> smoothing이 오히려 희귀 전문 용어의 변별력을 희석시키는 것으로 판단된다.
> BM25+의 delta 하한 보정이 이 컬렉션에서 더 안정적임을 실험으로 확인하였다.

#### 1-3. BPREF=0 질의 분석 및 한계 서술 예시

> 80개 질의 중 12개에서 BPREF=0.000이 관찰되었다. 주요 원인은
> 어휘 불일치(vocabulary mismatch)이다. 예컨대 Q74 "isotonic regression"의
> 경우 "isotonic"의 stemmed form인 "isoton"이 인덱스에 전혀 존재하지 않으며(df=0),
> Q65 "approximate message passing compressed sensing"의 관련 논문들은
> "AMP", "belief propagation" 등 쿼리와 다른 용어를 사용한다.
> document.txt 통계만으로는 이 어휘 간격을 메울 수 없어, 해당 질의들에서
> 성능 개선에 한계가 있었다.

---

### 2. OrGroup soft-factor 0.5 선택 근거 보강
- 현재: "0.3~0.7 변경 무효과"로만 기재
- 보완 예시:
  > q=0.5는 OrGroup에서 OR(q=0)과 AND(q=1)의 중간 지점으로, 질의어 일부 포함 문서에도
  > 점수를 부여하되 전체 포함 문서를 더 우대하는 균형 전략이다. 실험에서 0.1~0.9 범위의
  > 성능 차이가 없었으므로 이론적 중간값인 0.5를 기본값으로 유지하였다.

### 3. 수식 포맷 코드 블록으로 분리
- 현재: 수식이 한 줄로 붙어 가독성 저하
- 수정:

```python
normalized_tf = tf / ((1 - b) + b * (fl / avgfl))
score = idf * (k1 + 1) * (normalized_tf + delta) / (k1 + normalized_tf + delta)
if qf > 1:
    score *= (1 + log(qf))
```

---

## PART III — 분류 보완

### 1. NB vs SVM 성능 차이 이론적 근거 보충
- 현재: "어휘가 겹쳐 어렵다"는 도메인 해석만 있음
- 보완 예시:
  > NB(0.725)가 SVM(0.785)보다 낮은 이유 중 하나는, MultinomialNB가 feature independence를
  > 가정하는 반면 학술 텍스트에서는 용어 간 공기(co-occurrence) 패턴이 강해 이 가정이
  > 위배되기 때문이다. LinearSVC는 이러한 상관 구조를 마진 최대화 과정에서 암묵적으로
  > 처리할 수 있어 본 데이터에서 더 유리했던 것으로 판단된다.

### 2. 기각 방법 비교표 페이지 나누기 수정
- 현재: 비교표가 페이지 경계에서 두 조각으로 분리됨
- 수정: 단일 표로 합쳐서 재배치

---

## 우선순위 요약

| 우선순위 | 항목 | 영향 |
|---------|------|------|
| 🔴 높음 | 오탈자 (표 헤더 `confdence`, `lif` 등) | 감점 직결 |
| 🔴 높음 | 띄어쓰기 전체 점검 | 가독성·품질 점수 |
| 🟡 중간 | PART I 음의연관 나머지 3개 추가 | R1-2 보고서 점수 |
| 🟡 중간 | PART I 임계값 정성 해석 보강 | R1-2 보고서 점수 |
| 🔴 높음 | PART II 성능 개선 보고서 섹션 신규 작성 | 5점 (현재 0점) |
| 🟡 중간 | PART II OrGroup 근거 한 문장 | PART II 보고서 점수 |
| 🟡 중간 | PART III NB 이론적 근거 보충 | 보고서 품질 점수 |
| 🟢 낮음 | 수식 코드 블록 포맷 | 가독성 |
| 🟢 낮음 | 기각 방법 비교표 재배치 | 가독성 |
